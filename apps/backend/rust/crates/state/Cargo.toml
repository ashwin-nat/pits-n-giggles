[package]
name = "state"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "state"
path = "src/lib.rs"

[dependencies]
f1-types = { path = "../f1-types" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
